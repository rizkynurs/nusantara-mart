apiVersion: v1
kind: Service
metadata:
  name: { .Values.service.name }
  namespace: retail
spec:
  selector:
    app: order
  ports:
    - name: http
      port: { .Values.service.port }
      targetPort: { .Values.service.targetPort }
