image:
  repository: asia-docker.pkg.dev/PROJECT/nusmart/repo/content-service
  tag: "REPLACEME_SHA"

service:
  name: content-service
  port: 80
  targetPort: 8080

resources:
  requests: { cpu: "50m", memory: "128Mi" }
  limits:   { cpu: "200m", memory: "256Mi" }
